#!/usr/bin/env sh
# Simplified placeholder gradlew script.
# If you need the official Gradle Wrapper scripts/jar, let Android Studio regenerate them:
# Android Studio -> File -> New -> New Project (or use Gradle -> Wrapper task).
#
# Or install Gradle locally and run:
#   gradle wrapper --gradle-version 8.2
echo "This project omits gradle-wrapper.jar. Please open with Android Studio or regenerate the Gradle Wrapper."
exit 1
